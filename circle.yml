deployment:
  production:
    branch: gh-pages
    commands:
      - git config --global user.name  "Circle CI"
      - git config --global user.email ""
      - git commit --allow-empty -m "Deployed automatically"
      - git push
